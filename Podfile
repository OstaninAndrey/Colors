# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Colors!' do
  use_frameworks!

  # Pods for Colors!
  pod 'RealmSwift'
  pod "TableViewDragger"
end
